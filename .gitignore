# Credentials -- never commit
/config.php

# Local files
*.log
.DS_Store
.idea/

# The live-update signal, written by the application itself (LiveSignal);
# the temp file only shows up when a write was interrupted. The folder itself
# is under version control -- it carries the .htaccess that keeps everything
# but this one name from being served.
/assets/state/live.txt
/assets/state/live.txt.*.tmp

# Docker
/.env
.claude/

# AI tools
CLAUDE.md
AGENTS.md
